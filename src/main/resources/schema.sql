DROP TABLE IF EXISTS TBL_USUARIOS;
  
CREATE TABLE TBL_USUARIOS (
  id INT AUTO_INCREMENT  PRIMARY KEY,
  nome VARCHAR(100) NOT NULL,
  CPF VARCHAR(250) NOT NULL,
  tipoCONTA ENUM('admin','comum') DEFAULT 'comum'
);